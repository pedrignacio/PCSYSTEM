services:
  - type: web
    name: pcsystem
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.11.0
    healthCheckPath: /
    autoDeploy: true
